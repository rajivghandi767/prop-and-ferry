services:
  # -----------------------------------------
  # Database Layer
  # -----------------------------------------
  prop-ferry-local-db:
    image: postgres:18-alpine
    container_name: prop-ferry-local-db
    restart: unless-stopped
    env_file: .env.example
    networks:
      - prop-ferry-local
    ports:
      - "5432:5432"
    volumes:
      - prop-ferry-local-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U prop-ferry-dev -d prop-ferry-dev"]
      interval: 5s
      timeout: 5s
      retries: 5

  # -----------------------------------------
  # Caching Layer
  # -----------------------------------------
  prop-ferry-local-redis:
    image: redis:7-alpine
    container_name: prop-ferry-local-redis
    restart: unless-stopped
    networks:
      - prop-ferry-local
    ports:
      - "6379:6379"

  # -----------------------------------------
  # Django Backend (Smart Server)
  # -----------------------------------------
  prop-ferry-local-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: prop-ferry-local-backend
    env_file: .env.example
    networks:
      - prop-ferry-local
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    depends_on:
      prop-ferry-local-db:
        condition: service_healthy
      prop-ferry-local-redis:
        condition: service_started
    command: >
      sh -c "python manage.py migrate && 
             python manage.py seed_data && 
             python manage.py runserver 0.0.0.0:8000"

  # -----------------------------------------
  # React Frontend (Dumb Client)
  # -----------------------------------------
  prop-ferry-local-frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: prop-ferry-local-frontend
    env_file: .env.example
    networks:
      - prop-ferry-local
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - prop-ferry-local-backend

volumes:
  prop-ferry-local-db-data:

networks:
  prop-ferry-local:
